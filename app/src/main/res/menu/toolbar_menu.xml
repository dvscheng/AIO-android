<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Settings, should always be in the overflow -->
    <!--<item
        android:id="@+id/debug_toolbar_task_list"
        android:title="@string/task_list_toolbar"
        app:showAsAction="never"/>

    <item
        android:id="@+id/debug_toolbar_event_list"
        android:title="@string/event_list_toolbar"
        app:showAsAction="never"/>

    <item
        android:id="@+id/debug_toolbar_reminder_list"
        android:title="@string/reminder_list_toolbar"
        app:showAsAction="never"/>

    <item
        android:id="@+id/debug_show_notification_now"
        android:title="@string/notification_now_toolbar"
        app:showAsAction="never"/>

    <item
        android:id="@+id/debug_show_notification_delayed"
        android:title="@string/notification_delay_toolbar"
        app:showAsAction="never"/>-->

</menu>